<?php
    if (($this->session)){echo 'session is work  ';}
    echo 'congratulations, you are login!';
    $name = $this->session->get('name');
    if($name){
        echo '<br>'.$name;}
    else{echo 'DAAAA';}

//    echo '<br>'.$this->session->get('lifetime');
//$this->partial('login/login');
echo $this->tag->linkTo(
        "login/exit",
        "exit"
    );
$this->partial('menu/index');
?>